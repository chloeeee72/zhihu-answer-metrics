# Keep line endings stable across platforms.
# CI runs the .mjs / .yml / .js files on Linux; CRLF there breaks shebangs and shell steps.
* text=auto eol=lf

# Windows-only PowerShell keeps CRLF.
*.ps1 text eol=crlf

# Binary assets must never be normalised.
*.png binary
*.zip binary
